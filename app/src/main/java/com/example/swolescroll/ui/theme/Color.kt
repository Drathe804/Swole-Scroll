package com.example.swolescroll.ui.theme

import androidx.compose.ui.graphics.Color

// 1. The Parchment (Backgrounds)
val ParchmentLight = Color(0xFFF5E6D3) // Creamy paper color
val ParchmentDark = Color(0xFFE6D0B3)  // Slightly darker for Cards

// 2. The Iron (Primary / Buttons)
val IronBlue = Color(0xFF2C3E50)       // Strong, dark blue-grey
val IronDark = Color(0xFF1A252F)

// 3. The Ink (Text)
val InkBlack = Color(0xFF2D241E)       // Very dark brown, softer than pure black

// 4. The Gains (Accents)
val SwoleGold = Color(0xFFD4AF37)      // Metallic gold for highlights
val MuscleRed = Color(0xFF8B0000)      // Deep red for delete buttons/errors
